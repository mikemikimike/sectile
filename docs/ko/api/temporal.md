---
title: "@sectile/temporal API"
description: "@sectile/temporal의 공개 import 경로를 한 곳에서 확인합니다."
---
<!-- scripts/generate-component-pages.mjs에서 생성함. -->
# @sectile/temporal API

@sectile/temporal의 공개 import 경로를 한 곳에서 확인합니다.

사용 흐름과 예시는 [@sectile/temporal 가이드](/ko/packages/temporal)에서 먼저 확인하세요. 이 페이지는 정확한 공개 import 경로를 찾는 레퍼런스입니다.

## 설치

```sh
pnpm add @sectile/temporal
```

## 공개 import 경로

| Import | 관련 레퍼런스 |
| --- | --- |
| `@sectile/temporal` | 패키지 공개 entry point |
| `@sectile/temporal/date-field` | 패키지 공개 entry point |
| `@sectile/temporal/date-range-field` | 패키지 공개 entry point |
| `@sectile/temporal/date-time-field` | 패키지 공개 entry point |
| `@sectile/temporal/time-field` | 패키지 공개 entry point |
| `@sectile/temporal/time-range-field` | 패키지 공개 entry point |
| `@sectile/temporal/calendar` | 패키지 공개 entry point |
| `@sectile/temporal/date-picker` | 패키지 공개 entry point |
| `@sectile/temporal/date-range-picker` | 패키지 공개 entry point |
| `@sectile/temporal/range-calendar` | 패키지 공개 entry point |
| `@sectile/temporal/month-picker` | 패키지 공개 entry point |
| `@sectile/temporal/month-range-picker` | 패키지 공개 entry point |
| `@sectile/temporal/year-picker` | 패키지 공개 entry point |
| `@sectile/temporal/year-range-picker` | 패키지 공개 entry point |
| `@sectile/temporal/date-time-picker` | 패키지 공개 entry point |
| `@sectile/temporal/date-time-range-picker` | 패키지 공개 entry point |

`package.json`도 tooling용 공개 export로 제공됩니다.
