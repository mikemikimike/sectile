<!-- Generated by scripts/generate-component-pages.mjs. -->
# Time Field

Edit and validate a timezone-free wall-clock time.

## Usage

### Wall clock

Edit a timezone-free hour and minute value.

<ComponentExample component="time-field" scenario="wall-clock" title="Wall clock" description="Edit a timezone-free hour and minute value." :index="0" />

### Stepped

Accept values only at the configured interval.

<ComponentExample component="time-field" scenario="stepped" title="Stepped" description="Accept values only at the configured interval." :index="1" />

### Controlled

Let the parent own the current value and apply accepted changes back to the component.

<ComponentExample component="time-field" scenario="controlled" title="Controlled" description="Let the parent own the current value and apply accepted changes back to the component." :index="2" />

## Connect application state

Keep date and time values as structured civil or wall-clock data in `v-model` instead of converting them to `Date`. Your application decides if and when a timezone conversion is appropriate for storage or transport.

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { TimeField } from '@sectile/vue/temporal/time-field'

const value = ref({ hour: 9, minute: 30, second: 0, millisecond: 0 })
</script>

<template>
  <TimeField v-model="value" label="Start time" />
  <pre>{{ value }}</pre>
</template>
```

## API reference

See [Time Field API](/api/components/time-field) for props, events, slots, public types, part selectors, and keyboard behavior.

## Accessibility

The labeled input preserves native text entry while exposing time validation as one value.
