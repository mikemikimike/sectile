<!-- Generated by scripts/generate-component-pages.mjs. -->
# Date Time Picker

Choose a local date and time with coordinated field and calendar controls.

## Usage

### Schedule

Choose a deployment date and wall-clock time together.

<ComponentExample component="date-time-picker" scenario="schedule" title="Schedule" description="Choose a deployment date and wall-clock time together." :index="0" />

### Morning

Choose a morning date and time as one local schedule value.

<ComponentExample component="date-time-picker" scenario="morning" title="Morning" description="Choose a morning date and time as one local schedule value." :index="1" />

### Controlled

Let the parent own the current value and apply accepted changes back to the component.

<ComponentExample component="date-time-picker" scenario="controlled" title="Controlled" description="Let the parent own the current value and apply accepted changes back to the component." :index="2" />

## Floating positioning

This component uses the shared positioning engine. Use the [live positioning example](/guides/positioning) to change `side`, `align`, offsets, collision boundaries, strategy, and tracking while inspecting the resolved placement.

## Style available and selected dates

Style availability and selection from the public ARIA state on calendar cells. You do not need a second visual-only list that can drift from the component policies.

```css
[data-scope='date-time-picker'][data-part='cell'][aria-selected='true'] {
  background: CanvasText;
  color: Canvas;
}

[data-scope='date-time-picker'][data-part='cell'][aria-disabled='true'] {
  opacity: .45;
  text-decoration: line-through;
}
```

For SSR or tests that need a stable initial month, set the API `referenceDate` so the first rendered view does not depend on the current clock.

## API reference

See [Date Time Picker API](/api/components/date-time-picker) for props, events, slots, public types, part selectors, and keyboard behavior.

## Accessibility

Date and time inputs remain labeled while the popup calendar uses grid semantics and an explicit trigger.
